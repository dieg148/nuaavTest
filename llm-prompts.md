you are an expert in playwirght, I want to use some variables from a .env file from my test, it is a user and a password

how to create the storestate in playwright when there are cases to test negative flow in the login

seeing this: Error: Error reading storage state from playwright/.auth/user.json: ENOENT: no such file or directory

is it possible the auth.setup call the fixture?

"I have some troubles with the browser runner, I can't see spec test so I made some props for that at the end I realized it was a problem with the plugin"

I have a spec file with 3 tests inside a describe, first validate a lock user, second a non exists user and last one wrong pasword, checking the results, all tests are using same user, why? if I separate them into diferent describe, works as expected suing the right data

how to create a test with 2 set of data test.only('sort by price', async ({ }) => { // Check displayed products const option = 'hilo'; await productPage.sortByOption(option); await productPage.validateSortByPrice(option); });

do you now this error: Error: End of central directory record signature not found. Either not a zip file, or file is truncated.

how to call the users and password from the .env

